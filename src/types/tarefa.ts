export interface Itarefa {
    // inteface funciona como uma variável, onde colocamos o tipo dessa variável.
    tarefa : string,
    tempo: string,
    selecionado: boolean,
    completado: boolean,
    id: string
}